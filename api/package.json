{
  "name": "api",
  "version": "0.1.0",
  "description": "api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "docker:build": "docker build -t $npm_package_name .",
    "docker:dev": "docker run --rm --name $npm_package_name --net mybridge -it -p 3003:3000 -v `pwd`/src/:/usr/src/app/src/ $npm_package_name npm run dev",
    "docker:prod": "docker run --rm --name $npm_package_name --net mybridge -it -p 3004:3000 $npm_package_name"
  },
  "keywords": [],
  "author": "Sergio Miranda",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.7.0"
  }
}
